{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.place) }}
    {{ form_row(form.plannedAt) }}
    {{ form_row(form.tags) }}
    {{ form_row(form.link) }}

    <input type="submit" />
{{ form_end(form) }}
